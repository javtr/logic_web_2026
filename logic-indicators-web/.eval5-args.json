{"function": "() => { const region = document.querySelector('[aria-roledescription=carousel]'); const viewport = region.querySelector('.overflow-hidden'); const track = viewport.firstElementChild; const cards = Array.from(track.children); const dots = Array.from(region.querySelectorAll('button[aria-label^=\"Go to slide\"]')); return JSON.stringify({ trackInlineTransform: track.style.transform, trackComputed: getComputedStyle(track).transform, cardCount: cards.length, cardOffsets: cards.map(c => { const r = c.getBoundingClientRect(); const v = viewport.getBoundingClientRect(); return { relLeft: Math.round(r.left - v.left), relRight: Math.round(r.right - v.left), width: Math.round(r.width) }; }), viewportWidth: viewport.offsetWidth, activeDot: dots.findIndex(d => d.getAttribute('aria-current') === 'true') }, null, 2); }"}